import { TabPlaceholder } from "@/components/matters/tab-placeholder";

export default function MatterTimelinePage() {
  return (
    <TabPlaceholder
      title="Timeline"
      description="Chronological activity feed for this matter — filings, emails, evidence, deadlines, notes."
    />
  );
}
